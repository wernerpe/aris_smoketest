# Site install — the exact versions proved to work on 2026-09-15.
#
# WHY THIS FILE EXISTS.  `pyproject.toml` lists the dependencies UNPINNED,
# which is right for a development machine and wrong for a machine that is
# going to be set up once, on an installation day, by somebody who cannot
# debug a resolver.  These are the versions a clean venv actually resolved
# to on Ubuntu 24.04 / python 3.12.3, with
# `tests/test_gates.py tests/test_hardware_prep.py` green (20 passed) and a
# certified one-stroke plan on arm 31.  See docs/SITE_SETUP.md.
#
#     pip install -r requirements-site.txt
#     pip install -e .            # the repo itself, no deps to resolve
#
# This file is NOT a replacement for `pyproject.toml` and does not change it.
# If the site machine is not python 3.12, drop the pins and let pip resolve
# (section 5 of docs/SITE_SETUP.md) -- nothing here is load-bearing for
# correctness, it is reproducibility only.

# --- the planner's own dependencies (pyproject.toml [project].dependencies)
numpy==2.5.3
scipy==1.18.1
matplotlib==3.11.2
meshcat==0.3.2
trimesh==5.1.0
fast_simplification==0.2.0
shapely==2.1.2
networkx==3.6.1
rtree==1.4.1
pillow==12.3.0

# --- test runner (pyproject.toml [project.optional-dependencies].dev)
pytest==9.1.1
iniconfig==2.3.0
pluggy==1.6.0

# --- needed to BUILD the analytic-IK bindings, which are VENDORED at a pinned
#     commit under third_party/franka_analytical_ik (its VENDOR.md, and
#     docs/SITE_SETUP.md §1.3).  Install them right after this file:
#         pip install ./third_party/franka_analytical_ik
#     Also needs libeigen3-dev on the system; nothing is downloaded.
pybind11==3.1.0

# --- transitive, pinned so the resolve is reproducible
contourpy==1.4.0
cycler==0.12.1
fonttools==4.65.0
kiwisolver==1.5.1
packaging==26.3
pyparsing==3.3.2
python-dateutil==2.9.0.post0
six==1.17.0
tornado==6.5.10
u-msgpack-python==2.8.0
pyzmq==27.2.0
pyngrok==8.1.2
PyYAML==6.0.3
psutil==7.2.2
